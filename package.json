{
  "name": "keithah-com",
  "type": "module",
  "version": "1.0.0",
  "scripts": {
    "dev":     "astro dev",
    "build":   "npm run update:opensource && astro build",
    "update:opensource": "node scripts/update-opensource.mjs",
    "preview": "astro preview",
    "validate:pingscope": "npm run build && node scripts/validate-pingscope-product-page.mjs",
    "validate:wattline": "npm run build && node scripts/validate-wattline-product-page.mjs"
  },
  "dependencies": {
    "astro": "^7.1.1",
    "@astrojs/rss": "^4.0.19"
  }
}
